# su=1 continuation test: both segments use identical omega_b=π.
# If restore() works cleanly, velocity should be preserved (ux_rms≈0.25 after restart).
# Confirms whether the crash is specific to su≠1 rescaling or is a restore bug.
# Compare against chain_config_ellipse_smoke7.yaml (su=0.5, omega_b doubles).

fidelity: 7
geometry: {a: 0.20, b: 0.09, n: 2.0}
fill_level: 0.4

n_mix_cycles: 3
n_transition_cycles: 1
t_buffer: 5

sweep:
  parameter: omega_b
  values: [3.14159, 3.14159]

motion:
  n_harmonics: 1
  theta_max: [5.0, 0.0, 0.0]
  phi_angular: [0.3, 0.0, 0.0]
  omega_h: 6.28318
  amplitude_h: [0.02, 0.0, 0.0]
  phi_horizontal: [0.3, 0.0, 0.0]

videos: false
submit: true
walltime: "00:30:00"
